name: demo env

on:
 workflow_dispatch:
env:
  password: abhiraval
 
jobs:
  show:
    runs-on: ubuntu-latest
    steps:
     - name: show env 
       run: echo "may env var is ${{ env.password }}"
   

  
